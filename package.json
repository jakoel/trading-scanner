{
  "name": "watchlist-summary",
  "version": "1.0.0",
  "description": "Watchlist scanner — headless indicator engine (primary) with a legacy TradingView/CDP scanner kept for local cross-checking",
  "type": "module",
  "scripts": {
    "scan": "node legacy/scan_watchlist.js",
    "scan:headless": "node scan_headless.js"
  },
  "dependencies": {
    "chrome-remote-interface": "^0.33.2",
    "yahoo-finance2": "^4.0.0"
  }
}
