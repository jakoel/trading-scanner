{
  "name": "watchlist-summary",
  "version": "1.0.0",
  "description": "Standalone TradingView watchlist scanner — reads indicator data via CDP and sends alerts to Telegram",
  "type": "module",
  "scripts": {
    "scan": "node scan_watchlist.js"
  },
  "dependencies": {
    "chrome-remote-interface": "^0.33.2"
  }
}
